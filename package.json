{
  "name": "cypress-cdp-coverage",
  "version": "0.1.0",
  "description": "Cypress plugin for JS/CSS coverage using Chrome DevTools Protocol",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chrome-remote-interface": "^0.28.2"
  }
}
